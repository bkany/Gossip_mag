 <section class="jumbotron text-center">
        <div class="container">
        	<h1>Rajoute ton Potin !</h1>
        	
        	
        
    <%= form_tag url_for(action: 'create'), method: "post" do %>

      <%= label_tag 'Titre' %>
      <%= text_field_tag 'title', @potin.title %>
      <br /> <br />

      <%= label_tag 'Contenu' %>
      <%= text_area_tag 'content', @potin.content %>
      <br /> <br />

      <%= label_tag 'Utilisateur' %>
      <%= text_field_tag 'user_id', @potin.user_id %>
      <br /> <br />
      
      <%= select_tag 'tag', options_from_collection_for_select(Tag.all, :id, :title, selected = nil) %>
      <br /> <br />
      
      <%= submit_tag "Création du potin" %>

    <% end %>
        
</section>
